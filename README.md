# Wordpress-XML-RPC-Dictionnary
Wordpress exploit using a dictionnary based on XML-RPC vulnerability

Usage : python wpsxmlrpc.py -t http://target -l login -d dictionnary.txt [--threads number_of_threads]
